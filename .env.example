# Upstash Redis credentials for API rate limiting — https://console.upstash.com
# Copy this file to .env.local and fill in real values to enable 10 req/min/IP limits.
# When left blank, the rate-limit middleware passes through (fine for local dev).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
